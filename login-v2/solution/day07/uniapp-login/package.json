{
  "name": "login-v2-solution-day07-uniapp",
  "version": "1.0.0",
  "private": true,
  "description": "Day 07 参考答案（前端）：登录 / 注册（验证码）/ 忘记密码 / 重置密码 —— uni.request 只出现在 infrastructure",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "format:check": "prettier --check \"src/**/*.ts\" \"tests/**/*.ts\"",
    "gate": "npm run format:check && npm run typecheck && npm test"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.0",
    "vitest": "^3.2.7",
    "vue": "^3.4.0"
  }
}
