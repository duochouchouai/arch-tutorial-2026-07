/**
 * @file 时间源端口 — 领域与用例唯一允许的「现在」来源
 * @author 教程组
 *
 * 为什么禁止实体里直接 `Date.now()`：
 * 1. 测试无法控制时间 —— 断言「30 分钟后解锁」「验证码 5 分钟后过期」必须有假时钟；
 * 2. 时间是不可控的外部输入（和数据库、网络同级），属于基础设施，必须从边界注入；
 * 3. 「时间从哪来」是可以在组合根一眼看穿的装配事实，不该藏在业务方法里。
 */
export interface TimeProvider {
  /** 当前时间（Unix 毫秒） */
  now(): number
}
